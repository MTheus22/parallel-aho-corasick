# Build artifacts
build/
*.o
*.d
*.a
*.so
*.dSYM/

# Sanitizer / debugger outputs
core
core.*
vgcore.*
*.gcno
*.gcda
*.gcov

# Profiling output (perf, gprof, vtune, callgrind)
perf.data*
gmon.out
callgrind.out.*
*.qdrep
*.qdstrm

# Raw downloaded data files (recreated by scripts/)
rules/
*.rules
*.tar.gz
*.tar.bz2
*.xml.bz2
data/*

# Keep this README versioned (pattern lists are generated by scripts/)
!data/README.md

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~
*.DS_Store

# Language server / clangd
.cache/
.clangd/
compile_commands.json

# Python bytecode (scripts/)
__pycache__/
*.pyc

# Ignora o conteúdo de runs/ (logs grandes, regeneráveis), MAS deixa o
# diretório re-incluível abaixo. Usa runs/* (não runs/) porque o git não
# consegue re-incluir um caminho cujo diretório-pai foi excluído.
runs/*
# Exceção: os resultados do sweep da workstation SÃO versionados (corrida única
# numa máquina alugada — logs/env/sweep.db pequenos e irreproduzíveis). Os dados
# de corpus/padrões continuam ignorados por estarem em data/.
!runs/workstation/
# Sweep run logs (regenerated by scripts/run_*_sweep.sh)
*_sweep.log
overnight.out
workstation.out
